<script lang="ts">
	import '../app.css'

	// import { page } from '$app/stores'
	// import { reportVitals } from '$lib/vitals'
	// import { inject } from '@vercel/analytics'
	// import { onMount } from 'svelte'

	// export let data

	// onMount(() => {
	// inject()
	// reportVitals(data.VERCEL_ANALYTICS_ID, $page.url.href, $page.route.id || $page.url.href)
	// })
</script>

<slot />
